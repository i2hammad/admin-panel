'use strict';

/**
 * elimination-calendar router
 */

const { createCoreRouter } = require('@strapi/strapi').factories;

module.exports = createCoreRouter('api::elimination-calendar.elimination-calendar');
