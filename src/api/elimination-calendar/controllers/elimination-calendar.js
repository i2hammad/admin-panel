'use strict';

/**
 * elimination-calendar controller
 */

const { createCoreController } = require('@strapi/strapi').factories;

module.exports = createCoreController('api::elimination-calendar.elimination-calendar');
